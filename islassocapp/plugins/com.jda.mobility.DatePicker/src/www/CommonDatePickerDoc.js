/**
 * Plugin class that provides access to the native date pickers.
 *
 * ## Show date picker
 *     Jda.mobility.plugins.DatePicker.show( options, function(){onSuccess();} );
 *
 * @class Jda.mobility.plugins.DatePicker
 */

/**
 * @method show
 * Displays the native date picker.
 * @param {Object} options Key:Value set of options. If you provide a date in
 * options, it must be a Date object.
 * @param {Function} [callback] Callback to handle the returned date object.
 * if none is provided a default no-op function is supplied.
 */
